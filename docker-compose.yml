services:
  nginx:
    image: nginx:stable-alpine
    ports:
      - '8088:80'
    # 8443 removed: Windows Docker port binding issue after container restart
    depends_on:
      - gateway
    volumes:
      - ./config/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./config/locations.conf:/etc/nginx/conf.d/locations.conf:ro

  gateway:
    build:
      context: ./services/gateway
    ports:
      - '4000:4000'
    environment:
      - PORT=4000
      - NODE_ENV=${NODE_ENV:-development}
      - DATABASE_URL=postgres://postgres:${POSTGRES_PASSWORD}@postgres:5432/abc_io
      - JWT_SECRET=${JWT_SECRET}
      - OWNER_SIGNING_KEY=${OWNER_SIGNING_KEY}
      - STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
      - STRIPE_WEBHOOK_SECRET=${STRIPE_WEBHOOK_SECRET}
      - STRIPE_PRICE_ID_FREE=${STRIPE_PRICE_ID_FREE}
      - STRIPE_PRICE_ID_BASIC=${STRIPE_PRICE_ID_BASIC}
      - STRIPE_PRICE_ID_STANDARD=${STRIPE_PRICE_ID_STANDARD}
      - STRIPE_PRICE_ID_PRO=${STRIPE_PRICE_ID_PRO}
      - STRIPE_PRICE_ID_BUSINESS=${STRIPE_PRICE_ID_BUSINESS}
      - STRIPE_PRICE_ID_TEAM=${STRIPE_PRICE_ID_TEAM}
      - STRIPE_PRICE_ID_CORPORATE=${STRIPE_PRICE_ID_CORPORATE}
      - STRIPE_PRICE_ID_ENTERPRISE=${STRIPE_PRICE_ID_ENTERPRISE}
      - STRIPE_PRICE_ID_AGENCY=${STRIPE_PRICE_ID_AGENCY}
      - STRIPE_PRICE_ID_GLOBAL=${STRIPE_PRICE_ID_GLOBAL}
      - PAYPAL_CLIENT_ID=${PAYPAL_CLIENT_ID}
      - PAYPAL_CLIENT_SECRET=${PAYPAL_CLIENT_SECRET}
      - PAYPAL_WEBHOOK_ID=${PAYPAL_WEBHOOK_ID}
      - PUBLIC_URL=${PUBLIC_URL:-https://abc-io.com}
      - CORS_ORIGIN=${CORS_ORIGIN}
      - SMTP_URL=${SMTP_URL}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
      - GATEWAY_API_KEY=${GATEWAY_API_KEY}
      - SELF_HEAL_TOKEN=${SELF_HEAL_TOKEN}
      - REDOT1_API_KEY=${REDOT1_API_KEY}
    depends_on:
      - postgres
      - kimi
    restart: unless-stopped

  operator-station:
    build:
      context: ./services/operator-station
    ports:
      - '8080:8080'
    depends_on:
      - gateway

  owner-dashboard:
    build:
      context: ./services/owner-dashboard
    ports:
      - '8500:8500'
    environment:
      - OWNER_SIGNING_KEY=${OWNER_SIGNING_KEY}
      - OWNER_SIGNING_FINGERPRINT=${OWNER_SIGNING_FINGERPRINT}
      - OWNER_ACCOUNT_EMAIL=${OWNER_ACCOUNT_EMAIL}
      - OWNER_ACCOUNT_PASSWORD=${OWNER_ACCOUNT_PASSWORD}
      - OWNER_SESSION_TOKEN=${OWNER_SESSION_TOKEN}
      - OWNER_BIOMETRIC_SECRET=${OWNER_BIOMETRIC_SECRET:-}
      - MOBILE_GATEWAY_URL=http://mobile-gateway:5050
      - APK_PATH=/apk/redot2-latest.apk
      - GIT_DEFAULT_BRANCH=${GIT_DEFAULT_BRANCH:-main}
    volumes:
      - ./apk:/apk:ro
    restart: unless-stopped
    depends_on:
      - gateway
      - mobile-gateway

  mobile-gateway:
    build:
      context: ./services/mobile-gateway
    ports:
      - '5050:5050'
    environment:
      - MOBILE_SIGNING_KEY=${MOBILE_SIGNING_KEY}
      - MOBILE_SIGNING_FINGERPRINT=${MOBILE_SIGNING_FINGERPRINT}
    restart: unless-stopped
    depends_on:
      - redis

  public-portal:
    build:
      context: ./services/public-portal
    # Host port 8090 removed: access via NGINX proxy only (Windows port binding issue)
    environment:
      - DATABASE_URL=postgres://postgres:${POSTGRES_PASSWORD}@postgres:5432/abc_io
      - PUBLIC_SIGNING_KEY=${PUBLIC_SIGNING_KEY}
      - PUBLIC_SIGNING_FINGERPRINT=${PUBLIC_SIGNING_FINGERPRINT}
      - JWT_SECRET=${JWT_SECRET}
    restart: unless-stopped
    depends_on:
      - gateway

  beacon-pwa:
    build:
      context: ./services/beacon-pwa
    ports:
      - '3005:3000'
    restart: unless-stopped
    environment:
      - NODE_ENV=production

  account-pwa:
    build:
      context: ./services/account-pwa
    ports:
      - '8100:3000'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - GATEWAY_URL=http://gateway:4000
    depends_on:
      - gateway

  kimi:
    build:
      context: ./services/kimi
    ports:
      - '5000:5000'
    environment:
      - AI_PROVIDER=${AI_PROVIDER:-mistral}
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
      - MISTRAL_MODEL=${MISTRAL_MODEL}
      - MISTRAL_API_BASE_URL=${MISTRAL_API_BASE_URL}
      - KIMI_API_KEY=${KIMI_API_KEY}
      - KIMI_MODEL=${KIMI_MODEL}
      - KIMI_API_BASE_URL=${KIMI_API_BASE_URL}
    restart: unless-stopped

  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: abc_io
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    restart: unless-stopped
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./services/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql:ro

  prometheus:
    image: prom/prometheus:latest
    ports:
      - '9091:9090'
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    restart: unless-stopped

  grafana:
    image: grafana/grafana:latest
    ports:
      - '14000:3000'
    restart: unless-stopped

  redis:
    image: redis:alpine
    ports:
      - '6379:6379'
    restart: unless-stopped

  worker:
    build:
      context: ./services/worker
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - REDIS_DB=0
    depends_on:
      - redis
    restart: unless-stopped

  logger:
    image: busybox
    command: sh -c 'while true; do sleep 3600; done'

  tracer:
    image: jaegertracing/all-in-one:latest
    ports:
      - '16686:16686'
      - '6831:6831/udp'

  headscale:
    image: headscale/headscale:latest
    container_name: redot2-headscale
    ports:
      - '8085:8080'
      - '9095:9090'
      - '41641:41641/udp'
    volumes:
      - ./config/headscale:/etc/headscale:ro
      - headscale-data:/var/lib/headscale
    command: serve
    restart: unless-stopped

  ai-isp:
    build:
      context: ./services/ai-isp
    ports:
      - '7000:7000'
    environment:
      - PORT=7000
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  beacon:
    build:
      context: ./services/beacon
    ports:
      - '3006:3000'
    environment:
      - NODE_ENV=production
      - PORT=3000
      - DATABASE_URL=postgres://postgres:${POSTGRES_PASSWORD}@postgres:5432/abc_io
      - SMTP_URL=${SMTP_URL}
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      - OWNER_ACCOUNT_EMAIL=${OWNER_ACCOUNT_EMAIL}
    restart: unless-stopped

  autonomous:
    build:
      context: ./services/autonomous
    environment:
      - CHECK_INTERVAL_SECONDS=30
      - REDIS_URL=redis://redis:6379/0
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /opt/redot2:/opt/redot2:ro
    depends_on:
      - redis
    restart: unless-stopped

volumes:
  postgres-data:
  headscale-data:
